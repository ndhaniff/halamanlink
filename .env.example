# App
APP_DOMAIN=localhost
APP_URL=http://localhost:4321
SESSION_SECRET=change-me-to-a-long-random-string

# Stripe (optional for local dev)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PRO_MONTHLY=price_...

# Google Maps Embed API (for pinned location on public profiles)
# Enable "Maps Embed API" in Google Cloud Console. Billing required; ~$200/mo free credit.
GOOGLE_MAPS_API_KEY=

# Production SQLite file path (required for astro build in Docker)
ASTRO_DATABASE_FILE=./data/content.db

# Uploaded avatars (optional — defaults to ./data/uploads)
# UPLOAD_DIR=/app/data/uploads
